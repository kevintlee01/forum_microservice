import { DataType } from '../types';
export default function shouldUpdateWithError({ errors, name, error, isOnBlur, isBlurType, isValidateDisabled, }: {
    errors: DataType;
    name: string;
    error: any;
    isOnBlur: boolean;
    isBlurType: boolean;
    isValidateDisabled: boolean;
}): boolean;
