import { DataType, FieldErrors, ValidationReturn } from '../types';
export declare function parseErrorSchema(error: DataType): FieldErrors;
export default function validateWithSchema(ValidationSchema: any, data: DataType): Promise<ValidationReturn>;
